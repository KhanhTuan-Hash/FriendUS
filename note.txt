""" NOTE: STILL FIXING, USE LOCALHOST RN
if __name__ == '__main__':
    with app.app_context():
        db.create_all()
        populate_db() 
    
    print("Server running! access it at http://<YOUR_IP_ADDRESS>:5000")
    # Whelxi note: when hosting, remember to set debug=False and binding host='0.0.0.0'
    socketio.run(app, host='0.0.0.0', port=5000, debug=False)
"""